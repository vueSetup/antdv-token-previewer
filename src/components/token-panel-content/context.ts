// import { ref, type Ref, type InjectionKey } from 'vue'
// import { createContext, useContext } from '../../composables/useContext'

// type Context = { prefixCls: string; hasId: Ref<string> }

// const key: InjectionKey<Context> = Symbol('locale')

// const StyleContextProvider = createContext<Context>()

// export const useStyleContext = () =>
//   useContext<Context>(key, {
//     prefixCls: 'token-panel-pro',
//     hasId: ref('')
//   })

// export default { Provider: StyleContextProvider }
